class Solution {
public:
    vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {
        vector<int> ans ;
        stack<int> s;
        map<int, int>m;

        for(int i= nums2.size()-1 ; i >=0; i--){

            while(s.size()> 0 && s.top() <= nums2[i]){
                s.pop();                                // if top value of stack iis small then pop the element 
            }

            if(s.empty()){                              // ifstack is empty store -1 ;
                m[nums2[i]] = -1;
            }
            else {
               m[nums2[i]] = s.top();                   // store peek value
            }
            s.push(nums2[i]);
        }

        for(int i=0 ; i<nums1.size(); i++){
            ans.push_back(m[nums1[i]]);
        }

        return ans  ;
    }
};
